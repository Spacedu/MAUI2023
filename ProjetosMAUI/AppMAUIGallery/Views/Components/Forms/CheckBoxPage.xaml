<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppMAUIGallery.Views.Components.Forms.CheckBoxPage"
             Title="CheckBoxPage">
    <VerticalStackLayout>
        <Label 
            
            Text="Qual a linguagem que você mais gosta?"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <HorizontalStackLayout>
            <CheckBox Color="Purple" IsChecked="False" CheckedChanged="CheckBox_CheckedChanged"/>
            <Label Text="PHP" VerticalOptions="Center" />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox Color="Blue" IsChecked="False" CheckedChanged="CheckBox_CheckedChanged"/>
            <Label Text="C#" VerticalOptions="Center"  />
        </HorizontalStackLayout>
        <HorizontalStackLayout>
            <CheckBox Color="Red" IsChecked="False" CheckedChanged="CheckBox_CheckedChanged"/>
            <Label Text="Java" VerticalOptions="Center"  />
        </HorizontalStackLayout>
        <Label Text="" x:Name="LblStatus" />
    </VerticalStackLayout>
</ContentPage>